<UserControl x:Class="WildsSim.Views.SubViews.ArtianTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WildsSim.Views.SubViews" xmlns:ctrl="clr-namespace:WildsSim.Views.Controls" xmlns:subviews="clr-namespace:WildsSim.ViewModels.SubViews" d:DataContext="{d:DesignInstance Type=subviews:ArtianTabViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  ShowGridLines="True">

        <!-- グリッド設定 -->
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!-- 登録 -->
        <StackPanel Grid.Row="0" Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="65" Text="武器種："/>
                <ComboBox Width="115" ItemsSource="{Binding WeaponTypes.Value, Mode=OneWay}" SelectedItem="{Binding SelectedWeaponType.Value}" SelectedIndex="0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="65">名前：</TextBlock>
                <TextBox Width="115" Text="{Binding ArtianName.Value}"/>
            </StackPanel>
            <StackPanel>
                <ItemsControl ItemsSource="{Binding ArtianSkillSelectorVMs.Value, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ctrl:SkillSelector/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <Button x:Name="artianAddButton" Content="登録する" Command="{Binding AddArtianCommand}" />
        </StackPanel>

        <!-- 一覧 -->
        <ctrl:RowDraggableDataGrid Grid.Row="0" Grid.Column="1" x:Name="grid" ItemsSource="{Binding Artians.Value, Mode=OneWay}" IsReadOnly="true" AutoGenerateColumns="False" RowChangedCommand="{Binding RowChangedCommand}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="武器種" Binding="{Binding ArtianWeaponType.Value, Mode=OneWay}" />
                <DataGridTextColumn Header="名前" Binding="{Binding DispName.Value, Mode=OneWay}" />
                <DataGridTextColumn Header="スキル" Binding="{Binding SkillDescription.Value, Mode=OneWay}" />
                <DataGridTemplateColumn IsReadOnly="True" Header="削除">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DeleteCommand}">削除</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </ctrl:RowDraggableDataGrid>
    </Grid>
</UserControl>
